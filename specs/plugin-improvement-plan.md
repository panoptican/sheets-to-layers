Sheets to Layers — implementation plan, September 12, 2026

Implement all 17 findings and all five improvement areas in the [plugin review](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/specs/plugin-review.md). Astra directs the work, resolves shared design decisions, and signs off on integration. Sol handles the sync pipeline and document mutations; Terra handles the interface, data interpretation, and network transport; Luna handles bounded verification infrastructure and documentation tasks.

This is a plan for subsequent execution. No implementation or deployment is authorized by creating it. The review remains the evidence snapshot; this file becomes the living execution checklist, with verification evidence added as work completes. The previous Plannotator review returned “User reviewed the document and has no feedback.”

Use R1–R17 for the report's numbered findings. E1–E5 refer, in order, to its optional improvements: integration tests/lint; preflight; performance; proxy controls/fallback disclosure; settings/documentation. Every item below is required scope, including E1–E5.

**Execution model and ownership**

- **Astra — orchestration, Git, and shared contracts.** Own this plan, [shared types](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/types.ts), and [message definitions/guards](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/messages.ts). Define interfaces before parallel implementation, review consequential mutations, resolve cross-module disagreements, and integrate each wave. Astra alone creates the working branch, stages changes, and makes commits. Delegate implementation in bounded work orders rather than assigning one agent per finding.
- **Sol — sync pipeline.** Own [main-thread handling](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/code.ts), [orchestration](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/sync-orchestrator.ts), [sync engine](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/sync-engine.ts), [traversal](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/traversal.ts), [repeat frames](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/repeat-frame.ts), [indexes](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/index-tracker.ts), and [component swapping](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/component-swap.ts). Own new metadata/preflight modules and corresponding unit tests.
- **Terra / interface — plugin UI.** Own [UI logic](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/ui/ui.ts), [styles](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/ui/styles.css), and any small UI modules extracted from them. Own UI-specific test cases while Luna owns the shared browser harness. Preserve the existing visual system and Figma theme integration.
- **Terra / data — interpretation and property application.** Own [parser](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/parser.ts), [sheet structure](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/sheet-structure.ts), [special types](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/special-types.ts), [image application](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/image-sync.ts), [text application](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/text-sync.ts), [font/performance helpers](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/core/performance.ts), and corresponding unit tests.
- **Terra / transport — fetching and Worker.** Own the sheet/Worker fetchers, fetcher factory/interface, [URL utilities](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/src/utils/url.ts), and [Worker source](/Users/jason/Projects/tools/figma-plugins/sheets-to-layers/worker/sheets-proxy.js), plus corresponding tests. Sol independently reviews the security-sensitive Worker changes before acceptance.
- **Luna — test infrastructure and documentation.** Own shared Figma test mocks, integration/browser/Worker harness setup, test scripts, lint configuration, package manifest/lockfile changes, and later README/designer/Worker guidance. Other agents write their own behavior tests. Luna does not decide sync semantics or security policy.

Run at most four implementation workers simultaneously, plus Astra. Reuse a worker for later related work. Terra/interface, Terra/data, and Terra/transport are distinct assignments and may be separate Terra agents when the schedule permits. No two workers edit the same file simultaneously. Shared-contract changes go through Astra; dependency changes go through Luna after Astra reviews them. Every dispatch states that others are working in the checkout, that unrelated edits must be preserved, and that the worker must report files changed, tests, limitations, and required handoffs. Astra owns this plan throughout; workers report status rather than editing it concurrently.

Keep the current checkout and use the branch/commit workflow below. No application framework, production dependency, CI/CD change, or new infrastructure is assumed. Small development-only additions for TypeScript linting and repeatable browser tests may be proposed explicitly during setup; record the exact packages before installing. Avoid a broad tooling upgrade.

**Git workflow — Astra owns the branch and every commit**

This incorporates the Plannotator feedback requesting Git tracking, a branch, and centralized committing. At plan revision, the checkout is on `main`, tracking `origin/main`, with only `specs/` untracked. No branch or commit has been created for this work yet.

- At the start of execution, before dispatching implementation workers, Astra rechecks the checkout and outstanding changes, records the starting commit, and creates `codex/sheets-to-layers-improvements` from the inspected `main` state in this checkout. Preserve pre-existing work; if the starting state has changed or the branch already exists, inspect it before proceeding and surface any consequential difference. Do not create separate worker branches or worktrees.
- Astra uses the `scoped-commits` skill to assemble small, coherent Conventional Commits. Begin by recording the reviewed plan and findings, then commit completed changes with their regression tests and relevant documentation. Split by independently understandable behavior or foundation, rather than one commit per agent or an entire wave. Keep mutually dependent contract/caller changes together when splitting would leave a broken intermediate state.
- Workers edit only their assigned files and report their changes and checks. They do not stage, commit, switch branches, merge, rebase, or otherwise mutate Git state. Before staging a chunk, Astra obtains a handoff and pauses edits to the affected files through validation and commit; unrelated workers may continue within their ownership boundaries.
- Astra reviews the exact staged diff, stages explicit files or hunks, excludes unrelated work, scans staged content for secrets, and runs the smallest meaningful checks for that chunk. Commit only passing work, with the verified content held unchanged until the commit completes. Where other unfinished changes affect validation, wait for a coherent passing integration point. Run the full required checks at the integration gates below. Temporary failing reproductions demonstrate defects during development; pair their durable regression tests with the corresponding fix instead of committing a known-failing default suite.
- Record each completed chunk's commit ID and verification evidence in this living plan. Keep commits local. Pushing, merging into `main`, and publishing remain separate release actions requiring the user's direction; do not rewrite published history. Starting implementation under this plan includes the stated branch creation and local commits without repeated permission requests for each chunk.

**Behavior decisions to carry into implementation**

1. **Document-owned sync configuration.** Store a versioned configuration on the Figma document: validated source URL/spreadsheet identity, default worksheet identity, scope, stable target roots, and interpretation preferences. A page scope records that page's ID, so re-sync does not move to whichever page is currently open. A document scope covers the document's pages; selection scope stores ordered selected roots after removing duplicates and descendants of another selected root. Persist roots, not the subset of leaves that changed. Keep raw worksheet contents in the transient UI snapshot, not document metadata. One last-used configuration per document is sufficient for this work; a multi-source configuration manager is not required.

2. **Conservative legacy migration.** Existing global client storage cannot establish which file owns its URL or layer IDs. Use the legacy URL only as an input suggestion. The next explicit sync creates document metadata; re-sync without trustworthy metadata opens the normal UI. Missing saved roots produce a warning and retain the original intended scope for review; never widen to a full page automatically. If all roots are missing, require a new explicit scope selection. A completed partial run retains the full intended roots for retry; cancellation does not replace the previously completed configuration.

3. **One sync pipeline and stable row semantics.** All modes resolve roots → actual ancestor context → worksheets/bindings → preflight/repeat changes → current traversal → values/properties → images → terminal result. Row counters advance for every eligible binding, including unchanged or failed applications, in the same deterministic order. Missing labels are diagnosed; they do not silently change the meaning of another binding. Preserve documented specific, increment, skip-blank, and random modes. A random row is resolved once per run and reused by its preflight/application; a new run may randomize again. Retain an immutable in-memory application plan keyed by run/snapshot ID and binding identity, including worksheet, resolved row, and values. Retry failed applications against that same snapshot after revalidating the targets, rather than restarting counters over the failed subset. Refresh or reopening starts a full new run over saved roots; it does not pretend to resume a discarded snapshot.

4. **A shared binding grammar.** UI edits round-trip through a parser/serializer instead of regex suffix surgery. Simple existing names retain their meaning; a displayed `First Name` can serialize as `#First_Name`, with the index last. Quote/escape labels or worksheet names that cannot be represented unambiguously by the simple grammar, and document the extension. Diagnose normalization collisions rather than selecting the first header. Browsing preview tabs does not silently change document bindings; start on the URL's active tab and provide an explicit default-worksheet choice and “apply worksheet” action.

5. **Explicit interpretation and empty-data behavior.** Default a one-column sheet to header-plus-values. Preserve explicit Rows/Columns preferences per source worksheet and allow the preview to recompute from retained raw data without fetching again. Default blank text cells to the existing clear-and-hide behavior; offer “leave text unchanged” as a saved option. Blank image/component values leave existing content unchanged and are reported as skipped. Missing/invalid repeat data never triggers removal. For a valid zero-row source, preserve the template and existing structure with a clear “no data; repetition skipped” warning in this release, rather than deleting the only reusable template.

6. **Controlled structural and image edits.** Property-only variants resolve inside the current component set; explicit component names may change families only when unambiguous. Repeat removals are calculated before mutation and shown in preflight. Target the first existing image paint, replacing its hash while preserving paint properties and other fills; if no image paint exists, append one default image paint without deleting other paints. If a user changes/deletes a pending image target, treat the response as conflicted/skipped rather than overwriting the user's new paint.

7. **One cancellable operation through image completion.** Give each run an ID before fetching and correlate all data, progress, error, cancellation, preflight, and image messages. Main thread owns lifecycle and mutation authority; UI owns network AbortControllers. Terminal states are success, partial, failed, or cancelled, with changed/unchanged/skipped/failed counts and structured details. Only the active run can mutate or save metadata. Cancel during fetching means zero document changes; cancellation during application stops subsequent work and honestly reports changes already made. Do not promise automatic whole-document rollback. Re-sync closes only after images settle, and stays open when user action or failure details need attention.

8. **Practical UX and security defaults.** Fetch remains a read-only preview. Fetch & Sync may proceed automatically when preflight is unambiguous and makes no removals; otherwise show the concrete proposed changes and issues before Apply. Unsafe/ambiguous affected operations cannot proceed until resolved or explicitly excluded. Keep a visible summary for partial results. Finish the settings interface, persist its preferences, and disable third-party CORS fallback by default; an explicit opt-in identifies the provider and the data it receives. No credentials belong in the client bundle or public document metadata.

**Wave 0 — establish contracts, tests, and the HTML fix**

Owners: Astra, Luna, Terra/interface. Start here; the other implementation assignments wait for the shared contracts.

- Astra establishes the working branch and records the planning artifacts using the Git workflow above before any implementation dispatch.
- Astra defines minimal types for document configuration, scope roots, raw/normalized worksheet data plus diagnostics, operation messages, preflight output, and per-layer outcomes. Document selection ordering, metadata write rules, partial results, and image acknowledgements. Implement shared declarations/guards in coordination with callers; avoid a second permanent compatibility protocol.
- Luna ports the useful temporary reproductions into durable tests that exercise actual modules together. Add main-thread message/storage tests, browser tests against the built UI, and Worker tests with injected upstream responses. Wave 0 builds the harnesses and reproductions of current behavior; the owning workers add/wire tests for new contracts as their later implementation lands. Keep only the Figma/network boundary mocked. Model removed nodes, clone IDs/parents, mixed fonts, and delayed/out-of-order messages realistically.
- Luna restores TypeScript-aware ESLint using the existing toolchain where practical, makes `test:int` useful, and provides explicit commands for browser and Worker tests. Do not weaken rules or delete tests to make checks pass. Preserve the passing baseline and record defect reproductions as the work queue. Demonstrate failures with the existing temporary reproductions; add each permanent regression to the default suite with its fix so the harness and HTML fix can be committed in passing chunks.
- Terra/interface fixes R1 across all interpolated untrusted values, not only table headers. Prefer DOM text/property setters; any retained templates use context-appropriate escaping. Use the browser harness to test quoted headers, worksheet names, URLs, titles, and injected event attributes.

Exit gate: the existing suite and committed new tests pass; remaining defect reproductions are recorded for later waves; the preview injection reproduction is fixed and protected by a passing browser regression; shared contracts are settled enough for parallel work. This is also the earliest point for a narrowly scoped security patch if separately requested.

**Wave 1 — shared data rules and the unified sync pipeline**

Owners: Sol and Terra/data, with Astra integrating shared contracts. Terra/interface can prepare pure result/preview components against those contracts; do not wire speculative messages.

- Terra/data implements the binding serializer, lossless header handling/duplicate diagnostics, active-worksheet resolution, raw-data retention/orientation overrides, and correct one-column interpretation. Add ordered dimension/position operations so `100s, 200w` and `200w, 100s` have intentional, tested results; independent X/Y instructions must both survive. Update property/text/image helpers to return structured outcomes and preserve image paints. Handoff these APIs before Sol integrates callers.
- Sol implements document metadata/migration, stable scope roots, ancestor-aware selection, one normal/re-sync pipeline, and full intended-target retention. Pass resolved worksheet information into repeat planning and variant context into swapping. Capture the source and preview data together so Preview → Sync cannot save another URL. Keep unchanged and failed bindings in row accounting and subsequent re-syncs.
- Sol makes repeat mutation safe: validate template/source/count first, plan additions/removals, process nested repeat structures in a defined order, and re-traverse current nodes after structural changes. Do not attempt to resurrect an already removed node by appending it. Stage reversible preparation before irreversible removals; failures/cancellation report the actual partial result. Validate Figma Undo behavior later in the live fixture.
- Sol refreshes descendant binding access after component swaps where the node structure changes. Sol and Terra/data converge on one image-application implementation rather than preserving duplicate paths with different paint behavior.

Exit gate: integrated tests prove two-file isolation, page identity, safe legacy/missing-root behavior, unchanged-first-row stability, inherited/ignored ancestry, repeat growth/shrink with the correct worksheet, zero-row template preservation with a warning, same-named variants in unrelated sets, successful explicit family changes and ambiguous-target rejection, parser round-trips, correct active tab, independent axes, one-column data, and crop/filter/multiple-fill preservation. Covers R2–R7, R11–R16, plus the data foundation for E2.

**Wave 2 — operation lifecycle, transport, and Worker hardening**

Owners: Sol and Terra/transport; Terra/interface wires lifecycle transitions. Luna extends fault fixtures where requested.

- Sol replaces loose pending flags with the agreed operation lifecycle, including cancellation before fetch, stale message rejection, preflight/apply identity, image queues/acknowledgements, and completion after image settlement. The UI disables conflicting starts; the main thread still rejects overlap and late messages independently. Retrying a failed subset must preserve source and resolved row choices.
- Terra/transport carries cancellation through both Worker and JSONP adapters. Remove script callbacks/listeners on cancellation, reject late callbacks safely, and bound fetch plus body consumption. Fresh Fetch/Re-sync must refresh values; shared in-flight requests may be deduplicated, while preview Sync deliberately uses its visible snapshot. Cache keys include source, worksheet, and interpretation; expose snapshot age and Refresh.
- Start with four concurrent image fetches and three concurrent worksheets, with at most six upstream requests in flight per plugin UI. Use a 15-second per-request deadline covering body reads, at most two retries for transient network/429/5xx failures, and a 90-second sheet-fetch deadline. Respect bounded Retry-After and cancellation. Benchmark/tune these named constants in Wave 4; avoid retrying permission/validation errors.
- Terra/transport quotes A1 references before encoding, validates spreadsheet IDs and URL hosts with a URL parser, and tests both values and formatting routes. Reject unsupported methods and protocols; validate redirect destinations, prevent obvious loopback/private/self-proxy targets, and verify what the hosting platform enforces rather than claiming a complete SSRF defense from a hostname regex. Validate image content and stream bytes with a proposed 20 MiB limit, including absent or false Content-Length. Confirm Figma-supported formats; unsupported formats get an actionable error. Bound sheet response/cell processing and show a clear limit error instead of silent truncation.
- Remove automatic third-party CORS fallback. Connect only the explicit opt-in path to settings, redact signed URLs from routine logs, and avoid logging credentials/upstream secret-bearing URLs. The intended image proxy remains public for validated public images under server-side limits; the Google API key is required only for Sheets operations. Arbitrary HTML/document responses are rejected. Document that client-side controls cannot protect a public proxy from outside callers. Sol reviews Worker validation, redirection, timeouts, and limits independently.

Exit gate: Cancel-before-fetch causes zero mutation/metadata writes; cancelling a new run preserves an existing completed configuration; cancellation during application/images stops subsequent mutations; late/duplicate/reversed messages cannot win; user-edited/deleted pending image targets are skipped visibly; partial worksheet/image failures remain visible; retry of a failed binding preserves its resolved row and successful neighbors; every terminal path settles and releases timers/listeners. Worker tests cover A1-like/spaced/apostrophe names, named-range collisions, methods, URLs, redirects, invalid content, streamed oversize bodies, timeout/retry, and missing secrets under the stated public-image policy. Covers R8–R10, R17 and E4's repository controls.

**Wave 3 — preflight, recoverable results, and finished settings**

Owners: Sol supplies preflight/application data; Terra/interface owns the complete UX; Luna checks documented flow coverage.

- Build a side-effect-free preflight from the same binding resolution used for application. Summarize scope/source, matched/unmatched bindings, duplicate headers, missing sheets/fonts, variant ambiguity, repeat additions/removals, row/orientation settings, and empty-cell policy. Plan generated children before touching the actual canvas. Revalidate targets and source/snapshot identity before Apply; if the document changed materially, refresh the summary rather than applying stale removals.
- Provide persistent progress, result, and error states with visible details and Back/Retry/Refresh actions. Counts reconcile to eligible layers; image failures and styling-only changes count correctly. Preserve results through re-rendering, selection changes, and resizing. Let a result select the affected layer where possible, without silently navigating or mutating unrelated content.
- Complete click-to-bind using structured actions and the shared serializer. Show the active/default worksheet distinction, retain a chosen row through worksheet edits, and support keyboard interaction. Provide orientation and blank-text behavior controls whose changes regenerate preview/preflight consistently.
- Render a discoverable settings control; persist the custom Worker URL and third-party fallback preference in client storage. Store document interpretation choices in document metadata. Validate Worker URLs, explain custom endpoint use, and restore preferences before starting re-sync network work. Keep the default endpoint for existing users unless explicitly changed.
- Use the existing Figma visual style, light/dark themes, clear focus treatment, keyboard navigation, and screen-reader status announcements. Keep errors and action buttons visible at both the normal and expanded window sizes.

Exit gate: built-UI tests cover preview → binding → preflight → apply → result → retry/re-sync, errors at every stage, settings persistence/reopen, orientation changes, quoted content, keyboard flows, and destructive-repeat review. The normal no-risk path stays short. Covers the UI portions of R7–R12 and E2/E5.

**Wave 4 — performance, documentation, and independent verification**

Owners: Terra/interface and Terra/data optimize their modules; Luna finalizes documentation/harnesses; Sol reviews security and document mutations; Astra verifies the combined result.

- Paginate the preview rather than adding a UI framework: start at 100 rows, add column paging when needed, and cap rendered cells around 2,000. Use delegated interactions that preserve keyboard access and focus across pages. Full data remains available for sync; the cap is presentation-only.
- Use a per-run font promise cache across batch loading, text updates, and special properties; cache successes/failures without repeating loads for every node. Share repeated image fetches while preserving per-node outcomes, and release bytes/cache entries after the run. Add cooperative yielding and cancellation checkpoints to traversal and repeat planning/application, not just the final layer loop.
- Establish benchmark fixtures for 100, 1,000, and 5,000 bound layers; many worksheet tabs; mixed fonts; repeated image URLs; and a 10,000-row preview. Record baseline/final elapsed time, peak in-flight requests, image fetch count, font loads, rendered cells, and cancellation response. Require the specified concurrency ceilings, at most 2,000 rendered cells, one in-flight fetch per distinct image URL, and one font-load promise per unique font per run. On the same fixture/host, compare five-run medians: a small-file regression exceeding the greater of 15% or 100 ms requires investigation before acceptance. Target cancellation acknowledgement within 250 ms outside an outstanding uninterruptible host API call, and record such host waits separately. Keep timing measurements in the benchmark/live record rather than flaky routine unit-test assertions.
- Luna updates README, Designer Guide, Worker instructions, changelog, verified setup links, and test/run commands to match actual behavior. Explain migration, indexing, explicit worksheet selection, preflight, blank cells, limits, proxy disclosure, settings, partial completion, and re-sync. Use one source for instructions and cross-link rather than duplicating long explanations.
- A Sol review checks code written by other owners for unsafe mutations and security regressions; Astra reviews Sol's mutation logic and reconciles all R/E coverage. Luna audits that each finding has a meaningful regression or explicit live verification item. Agents do not approve their own work solely because their unit tests pass.

Exit gate: typecheck, lint, unit/integration/Worker/browser suites, and production build pass on the integrated result. The plan records each R1–R17 and E1–E5 with named evidence or a concrete unresolved requirement. Unresolved implementation defects fail this gate; only the explicitly identified live/deployment checks remain pending for Wave 5. Covers E1, E3, and E5 completion.

**Wave 5 — live proof and release handoff**

Owner: Astra, with Sol for Figma issues and Terra/transport for the deployed Worker. This is a separate completion gate from local code readiness.

- Use a disposable Figma fixture and public test spreadsheet with known values, including two tabs, spaces/apostrophes/range-like tab names, duplicate headers, one-column data, missing fonts, repeated children, component sets sharing variant names, and cropped/multiple-fill images. Test fresh install/migration, two documents, selection/page/document scopes, restart/re-sync, row growth/shrink, cancellation, and failure recovery. Validate UI behavior and one-run Undo in Figma; mocks cannot establish those outcomes.
- Inspect the exact deployed Worker and applicable Cloudflare controls read-only first. Verify actual code/configuration, key restrictions, routes, request limits, and rate limiting. After an authorized deployment, check POST rejection, a controlled non-image URL rejection, valid public-image behavior under the intended key policy, response content types, and real quoted-tab Sheets requests. Use test fixtures and bounded requests; do not load-test production or probe private-network destinations. Use the 1Password skill and `op-agent` for any required secrets; never copy secrets into the plan, bundle, or logs. Do not assume the repository source is deployed.
- Prepare a concrete Worker deployment/configuration change and rollback artifact from the observed deployment. Include appropriate server-side rate/abuse controls; do not embed a shared secret in a public plugin as a substitute. If required controls need a paid plan or new infrastructure, present that exact tradeoff with a bounded alternative before changing it. The work is not fully complete while this deployment question remains unverified.
- Deliver the tested plugin artifacts, release notes, verification record, known limits, and rollback/recovery steps. Request any still-needed production deployment/publication authorization only after that package is concrete. Then, if authorized, deploy the verified target, run live endpoint and Figma checks, and record the resulting version. Publishing to Figma Community remains a distinct action with its own observed outcome.

Exit gate: local implementation, live validation, and deployment/publication status are reported separately. Do not label all work complete based only on the local suite or a Worker deployment receipt.

**Coverage and handoff rules**

R1 → Wave 0. R2–R6 → Wave 1. R7 → Waves 1/3. R8–R9 → Wave 2. R10 → Waves 1/2/3. R11–R12 → Waves 1/3. R13–R16 → Wave 1. R17 → Wave 2. E1 → Waves 0/4. E2 → Waves 1/3. E3 → Waves 2/4. E4 → Waves 2/5. E5 → Waves 3/4. Every corrected behavior is included in the Wave 5 live fixture where host or deployed-service behavior matters.

For each work order, Astra supplies: the R/E IDs; explicit file ownership; upstream contracts; required behaviors and non-goals; concrete tests; the expected handoff; and the instruction that Astra alone performs Git mutations. A worker reports completed and remaining items, commands/results, and any new dependency or product decision. If a task becomes a cross-module redesign, return it to Astra instead of spreading edits into another owner's files. Use Sol for a difficult correctness handoff, Terra for a bounded UI/data/transport implementation, and Luna for an exact test-infrastructure or documentation checklist. These are staffing assignments for this project, not claims about fixed model pricing or capability guarantees.

Execution status: [x] Wave 0, [x] Wave 1, [x] Wave 2, [x] Wave 3, [x] Wave 4, [ ] Wave 5. Local implementation and the integrated regression gate passed. Host-specific checks and the production deployment decision remain in Wave 5. Keep evidence and decisions here as work proceeds; do not create a second tracker for each agent.

**Execution record**

- September 12, 2026: execution authorized by the user's `/goal execute this plan` request. Inspected the existing checkout on `main` at `e91ab3cf3a2b92e791861b528163e65724e59edd`; only these two planning documents were untracked, with no staged or tracked changes. Created `codex/sheets-to-layers-improvements` in the same checkout. All commits remain local; deployment and publication require the separate release gate described above.
- Initial commit scope: `docs: record plugin review and implementation plan`, containing `specs/plugin-review.md` and this living plan. Baseline verification and implementation evidence follow below as completed.
- Planning artifacts committed as `d76fff5`. Starting `npm run typecheck` passed; `npm test` passed all 859 tests in 16 files. Exported immutable starting source/test fixtures to `/private/tmp/sheets-to-layers-improvements-baseline` for same-host performance comparison.
- Wave 0 development-only tooling additions: exact `@typescript-eslint/parser@6.13.2`, `@typescript-eslint/eslint-plugin@6.13.2`, and `playwright@1.63.0`; no production dependencies or framework changes. The infrastructure worker installed these before the requested prior handoff; orchestration caught this and requires prior review for further additions. ESLint configuration must retain recommended errors rather than silence existing violations.
- Tooling compatibility correction approved before installation: replace the two initial TypeScript ESLint packages with exact `8.70.0`. The actual lockfile has TypeScript `5.9.3` and ESLint `8.57.1`; registry peer metadata confirms `8.70.0` supports both. Preserve those existing compiler/linter versions and Playwright `1.63.0`.
- R1 initial evidence: production UI with attribute-context escaping passes `tests/browser/security.test.ts` (2 Chromium tests), covering malicious quoted headers/worksheet names, text values, and URL inputs. Full UI lifecycle coverage will extend this fixture with the final correlated protocol. Source patch is awaiting the coherent Wave 0 commit gate.
- Shared contracts now specify document configuration with ordered stable roots, transient source-bound raw-data snapshots, structured binding actions, preflight identities/issues/repeat removals, run-correlated image acknowledgements, and terminal per-layer counts. Main thread constructs snapshot source identity from its accepted request; network responses do not choose their own source identity.
- Wave 5 read-only deployment inventory: default endpoint is `https://sheets-proxy.spidleweb.workers.dev`; existing Wrangler authentication works. Current deployment `ccba0525-84fd-4ee5-90e7-66fb60362d5c` serves version `e8f9da5d-4911-41a0-9198-dc06b8965d6a` at 100%, deployed May 15, 2026. Version metadata reports compatibility date `2025-12-14` and only the `GOOGLE_API_KEY` secret binding; no rate-limit binding is listed. This establishes the rollback version, not complete account/route/WAF or Google key-restriction verification. No deployment or secret changes made.
- `c7a8bbc` — `test: add browser and host integration harnesses`. Exact staged files exported to `/private/tmp/sheets-to-layers-wave0-check` and verified independently of ongoing worker edits: typecheck passed, 866 tests across 19 unit/integration files passed, and the built-UI browser smoke test passed. This export is a temporary verification artifact, not another working branch or worktree.
- `4f2b446` — `fix: escape untrusted preview attributes`. Exact staged export passed typecheck and all three built-UI browser tests. Strict ESLint configuration remains an integration item until owners correct exposed findings; no recommended rules were disabled.
- Root review caught and returned additional implementation defects before acceptance: quoted parser control characters/force-include prefixes, prototype-like headers, dropped transport diagnostics, incomplete cancellation yields, concurrent image settlement, styling-only outcome counts, and Worker IPv6/deadline/status handling. Regression fixes and independent review remain part of the gate, rather than treating worker unit-test reports as signoff.
- Created a dedicated [disposable Figma verification file](https://www.figma.com/design/8sST5lhIZ0UC6aCJdOkXzP) in the sole connected Spidleweb plan. Initial inspection confirms an empty first page (`0:1`); no existing user design was changed. Fixture construction and actual plugin/Undo checks remain pending.
- Imported and metadata-verified a [synthetic six-tab test source](https://docs.google.com/spreadsheets/d/1jrFOY8AjyPioCjulKdZLW4_sdAoe8OdFD9BiY2kJ-3E/edit) into the existing ChatGPT folder. Tabs: Products (`574084565`), Q1 O'Brien (`377820435`), A1 (`200500809`), One Column (`1632794315`), Duplicate Headers (`1873081983`), Empty Data (`1672635166`). Local workbook previews checked; native visual check and public sharing are pending. The in-app browser is signed out and Arc is in active use, so browser takeover was avoided.
- `6a645d9` — `fix: preserve bindings and worksheet interpretation`. Data foundation exact staged export at `/private/tmp/sheets-to-layers-data-check` passed typecheck and all 890 unit/integration tests across 19 files, including existing engine consumers. Reviewed and secret-scanned 13 explicit files covering binding serialization, retained raw data/diagnostics, one-column interpretation, ordered geometry, font cancellation/cache, and paint preservation.
- Independent Worker review by Sol found no source blocker in the inspected basic routes, while identifying required missing fault-test coverage. Root also found a nested UI/Worker image-limiter deadlock; the owning worker is adding a built-UI regression. Rate binding/configuration, key restrictions, actual deployed source, and host validation remain release-gate work.

- `a81c54e` — `fix: bound and validate public proxy requests`. Four explicit Worker/config/test files reviewed and secret-scanned. Exact staged export at `/private/tmp/sheets-to-layers-worker-check` passed typecheck and 19 Worker tests. This is a local source/configuration commit, not a deployment.
- Retrieved the actual deployed Worker using read-only Wrangler dashboard import into `/private/tmp/sheets-to-layers-deployed-source/sheets-proxy`. Source SHA-256: `ab3ae9bc93273358df044b0c1e34d30c6da4ec74405d648907f9b3270ed8be17`. Observed no custom routes/domains; `workers_dev: true`, `preview_urls: false`, compatibility date `2025-12-14`, only `GOOGLE_API_KEY` secret, no rate binding, persisted invocation logs with query redaction disabled. Rollback archive (no secret values) is `dist/release/worker-rollback-2026-09-12/`; version rollback restores the legacy validation and query-logging risks documented there.
- Proposed `worker/wrangler.jsonc` preserves the observed endpoint/account/compatibility settings and adds `KNOWN_SELF_HOSTS`, query-string redaction, and a `RATE_LIMITER` binding with namespace `2026091201` and 600 requests per 60 seconds. Wrangler 4.130.0 dry-run passed. Sol independently checked the local schema, source binding, and rollback hash. A maximum 200-tab fetch makes 401 Worker GETs; images and retries share the remaining allowance. Cloudflare's rate binding is approximate and per IP/location, not an account-wide quota or complete abuse defense. Namespace availability/account eligibility remain deployment checks; no paid upgrade or deployment has occurred.
- Google Cloud Console read-only inspection of the credential named **Figma Sheets to Layers plugin** in project `spidleweb-cli` found API restriction **Google Sheets API** and application restriction **None**. No key value was revealed or changed. The named credential's value has not been compared with the opaque Worker secret; its name and creation date support identification but do not prove a value match.
- User enabled public-link Viewer access to the synthetic spreadsheet and made Chrome available. Native Sheets preview was inspected; six tabs and expected values are present. Bounded real requests through the current endpoint returned discovery, Products, Q1 O'Brien, One Column, and A1. The deployed legacy A1 route returned only `[["Title"]]` instead of the full A1-named worksheet, confirming the unquoted A1 defect live. The proposed code's quoted routes pass regressions; post-deployment verification is pending.
- Imported this checkout's manifest into Figma Desktop and opened the development plugin in the disposable file. Its real Fetch operation completed, showing Products/Alpha/Beta/Gamma and all six tabs. Native dark-theme preview was visually inspected. macOS automation intermittently loses plugin controls or returns `noWindowsAvailable`; user assistance has been requested for the Review sync click. Actual apply, Undo, restart/re-sync, two-file isolation, scope, variant, repeat, image, and cancellation host checks are still pending and are not inferred from mocks.
- Root/Sol review additionally replaced dynamic-page-incompatible synchronous `mainComponent` reads, guarded deep binding IDs and per-worksheet/source message caps, fixed cancellation during repeat cloning/removal, and corrected ignored/template repeat parsing. The combined suite reached 966 passing tests plus typecheck, lint, and production build before the final JSONP/UI transport corrections; the final integrated gate will be recorded separately.
- Same-host five-run performance evidence: original core medians at 100/1,000/5,000 layers were 11.553/35.291/72.439 ms; reviewed final pipeline measurements were 2.058/32.445/143.691 ms. The 5,000-layer increase is 71.252 ms, below the required greater-of-15%-or-100-ms investigation threshold. Cancellation acknowledgement was 1.233 ms with one attempted mutation after cancellation was queued, outside simulated host waits. Built-UI measurements observed three worksheet tasks, four image requests, six total upstream requests, six network fetches for 12 image messages sharing six URLs, two font loads for two distinct fonts, and 100 rendered value cells for a 10,000-row one-column preview. Baseline/final JSON evidence resides under `/private/tmp/sheets-to-layers-benchmarks/`; release copies and a final rerun follow integration.


**Integrated acceptance — September 12, 2026**

- `841b0b6` — `fix: make sheet and image transport bounded and cancellable`. Ten explicit transport files passed typecheck and 936 unit/integration tests on the exact staged export; this preceded the unified pipeline commit.
- `6f0e475` — `feat: review and apply document-scoped cancellable syncs`. The exact staged tree exported to `/private/tmp/sheets-to-layers-integrated-check` passed strict typecheck, recommended ESLint rules, **982 unit/integration tests in 22 files**, **16 built-UI browser tests in three files**, and production build. The 19 Worker checks are included in the unit/integration count. No production dependencies were added. Its generated `dist/code.js` and `dist/ui.html` were copied back unchanged for the live check.
- Root integration review found that explicit **Review sync** had incorrectly auto-applied an unambiguous preview, a fatal error could discard the terminal result, and multiple syncs in one open plugin needed explicit Undo boundaries. Sol corrected these before acceptance. Main-thread regressions now assert preflight before Apply, one truthful fatal terminal result, partial counts, and one `commitUndo()` per entered application. UI review also fixed cancellation terminal handling and rejection-safe image cleanup, with a controlled-clock stalled image-body deadline check.
- Root inspected light/dark screenshots at normal 720×320 and expanded 960×600 across input, preview, preflight, partial result, and settings. The compact preview initially hid all value rows; the final layout displays two rows. Its browser regression checks the second cell's viewport bounds and center hit-test. Screenshots are in `/private/tmp/sheets-to-layers-ui-qa/` and selected release copies are included in the package.
- `d655fca` — `test: benchmark sync limits and browser transport`. The benchmark now exits nonzero if the browser measurement is blocked. Five-run baseline/final source runs used the same corrected valid spreadsheet fixture ID and host (Node 24.21.0, macOS arm64). Baseline → final core medians at 100/1,000/5,000 layers: **11.256 → 2.104 ms**, **37.940 → 31.146 ms**, **71.466 → 141.120 ms**. The largest increase is 69.654 ms, below the 100 ms investigation threshold. These supersede the earlier provisional numbers.
- The combined UI/network/10,000-row preview benchmark improved from **1397.842 → 1166.687 ms**. Peak worksheet/image/upstream concurrency changed from **20/12/40 → 3/4/6**; 12 image messages sharing six URLs caused **12 → six** downloads; rendered value cells changed from **10,000 → 100**. Two distinct fonts caused two final load calls per run. Final cancellation acknowledged in **1.477 ms**, with one attempted binding at the queued cancellation boundary. Host waits are mocked and are not timing claims about Figma. Baseline and final engine pipelines differ, so these measurements establish resource/timing bounds, not functional equivalence.

**R/E regression coverage**

- R1: `tests/browser/security.test.ts` covers quoted attribute injection from worksheet names, headers, cells, and URL input against the built UI.
- R2: `tests/unit/document-config.test.ts` tests document ownership and invalid/foreign metadata; `tests/integration/main-thread-harness.test.ts` tests legacy URL suggestion and preservation after cancellation. Actual second-file isolation and legacy-source suggestion remain pending the second Desktop file.
- R3: `tests/unit/sync-engine.test.ts` — “counts unchanged layers and keeps their row position on the next full run”; saved stable roots and immutable retry tests cover changed/unchanged/failed neighbors.
- R4: engine tests retain actual ancestor worksheet/index context for selected descendants and skip ignored ancestors. Native selected-child scope preserved inherited Products row 2 in both full host runs.
- R5: engine and `tests/unit/repeat-frame.test.ts` cover inherited worksheet counts, virtual additions, explicit reviewed removals, and invalid/zero-row preservation.
- R6: engine tests keep property-only variants in the current family, allow unique explicitly named families, and block duplicate/ambiguous targets. Actual Button/Badge families sharing Size=Small/Large passed in Figma.
- R7: main-thread test “preserves the preview source URL through Sync and saves document-owned roots”; built-UI end-to-end test fetches, reviews, applies, and inspects saved metadata.
- R8: main-thread and end-to-end browser tests cancel held fetches, preserve nodes/configuration, and ignore late data; transport tests abort queued requests and body reads. Native cancellation during a held real fetch preserved configuration. A separate native Apply run cancelled pending images and rejected the subsequently released real image response without changing fills or saved configuration.
- R9: engine tests reject changed image paints and stale retry targets; main-thread tests wait for concurrent image replies. Browser tests exercise matching-run cancellation and request identities.
- R10: `tests/browser/end-to-end.test.ts` exercises actual built UI → main handler → engine messages with a failed image and successful retry; main-thread tests assert one structured fatal/partial terminal result and structured fetch errors. Browser tests verify visible terminal and recovery states. Immutable random row retry is tested in the engine.
- R11: `tests/unit/parser.test.ts` covers quote/escape/force-include round trips; UI browser tests use structured binding actions and preserve row selection through worksheet edits.
- R12: fetcher tests preserve URL gid/default identity and diagnose unavailable requested tabs; UI tests separate browsing from explicit default worksheet selection.
- R13: `tests/unit/special-types.test.ts` covers chained width/height and independent X/Y. Actual `/120x,80y` applied x=120/y=80 without changing width/height in Figma.
- R14: `tests/unit/sheet-structure.test.ts` covers one-column header-plus-values and retained raw-data reorientation. Real One Column fetch returned its known data.
- R15: engine repeat tests cover growth/shrink through the shared pipeline used by re-sync. Native separate-invocation re-sync followed actual Products row changes 3→4→2→3; shrink required explicit review of two removals per repeat group.
- R16: `tests/unit/image-sync.test.ts` covers first-image-paint replacement while preserving other fills. Actual three-card image sync preserved CROP, transform, contrast/saturation filters, opacity, and the separate solid fill.
- R17: `tests/unit/worker-proxy.test.ts` and `tests/integration/worker-harness.test.ts` cover quoted A1-like, spaced, and apostrophe worksheet names. The deployed legacy A1 defect was reproduced; the corrected route still needs authorized deployment and live verification.
- E1: strict lint, mock fidelity, main-thread integration, actual built-UI/main bridge, and Worker fault tests passed the integrated gate.
- E2: shared preflight/application tests cover missing roots/labels/fonts, variant ambiguity, destructive repeat review, explicit exclusions, and stale document changes. The corrected core on a genuine unavailable Arial Bold host fixture returned a blocking missing-font issue without text or structural changes; actual text sync likewise preserved the node on font-load rejection.
- E3: five-run benchmark JSON and UI pagination regression establish the specified local timing, concurrency, deduplication, font-load, and rendering bounds.
- E4: default-off third-party fallback, settings disclosure, bounded Worker source, independent Sol review, observed deployment archive, and dry-run passed. The production rate binding, post-deploy endpoint checks, and deployment authorization remain open.
- E5: README, Designer Guide, development and Worker instructions, changelog, settings persistence, keyboard focus, themes, and normal/expanded layout checks match the final implementation. Setup links were checked against official Google/Cloudflare documentation.

**Live gate — current observed state**

- Real public Sheets Fetch showed all six tabs and Alpha/Beta/Gamma in the native development plugin. Two actual syncs created three repeated cards. The latest image run set all three image hashes to `1737d3dfb411c07b86ed8bd30f5987a4dc397cc1` while retaining crop/filter/opacity/additional-fill properties. Property-only Button Size=Large and explicit Button/Size=Large from the Badge family both resolved to Button component `11:3`. Ignored text remained “Ignored original”; the second page remained “Second page original” after page-scoped sync.
- One native Figma Edit → Undo restored the first completed run from three cards/Alpha back to the single original template/Original title. This early observation preceded the per-run `commitUndo()` addition; both later complete host runs verified native Undo and a second Undo preserving the preceding selection run.
- A second disposable [isolation verification file](https://www.figma.com/design/ezMuJQPymiVq2z8BTSGo2o) contains only the synthetic “Second file untouched” text. No existing user design was altered. Cross-file plugin behavior is pending.
- Attempting to construct a missing-font fixture through Figma's API was rejected because an unloaded font cannot be assigned. The node still uses available Inter and was renamed accordingly; this is not a missing-font pass.
- Figma development hot reload restarts the plugin whenever build output changes. Local builds are now finished and the imported final assets are held unchanged. macOS CUA still intermittently hides the plugin's input frame and reports `noWindowsAvailable` for coordinate clicks, despite providing screenshots. Native menus remain accessible. The user completed the separate host-driver import. Native commands run and their reports can be read from the file; macOS automation still intermittently returns a blank screenshot or stale controls. Opening the second disposable file in Desktop remains a requested manual step.
- Production Worker is unchanged at version `e8f9da5d-4911-41a0-9198-dc06b8965d6a`. Proposed rate limits use Cloudflare's native binding; no paid upgrade or new service is authorized or performed. Official [binding documentation](https://developers.cloudflare.com/workers/runtime-apis/bindings/rate-limit/) describes positive integer namespace IDs, approximate per-location counters, and shared state for reused IDs; it does not provide a namespace inventory or establish this account's eligibility. An authorized deployment must stop on an eligibility/conflict error rather than upgrading the account or dropping the binding silently.

- `13dadba` — `docs: explain sync review recovery and proxy limits`. The five user/developer/Worker guides were reviewed, checked for valid local links, and secret-scanned. The prepared release candidate is `dist/release/sheets-to-layers-2026-09-12/`, including the plugin ZIP, exact Worker proposal, observed rollback archive, benchmark/test evidence, selected UI screenshots, and a snapshot of this verification record. Production asset SHA-256: code `c7621305a90b51947b8c12dad247726673aa0304639011bc58d66fbda5faafd4`; UI `d20bda72e1e640708af5714b59eebd884e864a7ab12e56d12c7c641a588b4855`. No push, deployment, account upgrade, or Community publication has occurred.

- Luna independently audited the final R/E mapping and release evidence. Every item has a named regression or explicit live/deployment gap. The audit found a missing R11 browser row-preservation assertion; root added and committed `47b5372` with “keeps a clicked data row when applying a worksheet binding before sync” to `tests/browser/end-to-end.test.ts`. The four-test bridge suite passed, including click row 2 → keyboard Apply worksheet → Review/Apply → actual “Second row” text. There are now 17 distinct passing browser cases across the integrated and focused follow-up runs.
- Release traceability was strengthened with separate typecheck/lint/build logs from the unchanged exact-stage export; rebuilt assets compare byte-for-byte with the release candidate. `evidence/native-figma-observation.json` captures a read-only current-node inspection of the native image/variant/X/Y/repeat results. Earlier Undo and interaction sequences remain narrative session observations, not saved replay traces.

- A separate disposable host driver is prepared at `/private/tmp/sheets-to-layers-host-driver/manifest.json`, with a release evidence copy under `evidence/host-driver/`. It wraps byte-for-byte copies of the accepted production assets, fails closed outside two known synthetic fixture fingerprints, and provides native menu commands for scope/repeat/Undo/retry/cancel checks, a separate saved-page re-sync invocation, and second-file isolation. The marked image-failure and delayed-response conditions are injected; successful recovery uses the real public Worker. Static composition/syntax checks passed. This preparation checkpoint preceded import and execution; actual host results and the subsequent supported-property correction are recorded below.
- Package completeness follow-up: added the MIT license and the development/Worker documents referenced by the plugin guides, together with the referenced Worker source/configuration. The release archive and file-hash manifest are regenerated from the final package; source and Worker publication statuses remain unchanged.


**Native host continuation — September 12, 2026**

- The user imported the separate host driver. Its first actual run on accepted source `6f0e475` passed public fetch, default settings, Review without mutation, page scope, repeat shrink 5→3 and growth 1→3, native Undo, selected-child inherited row 2, document scope, Undo preserving the preceding selection run, image failure/retry preserving resolved row 2, fetch cancellation preserving configuration, and restored settings. `evidence/native-host-verify.json` contains the complete report read from native text node `17:35` on report page `17:14`. The image failure and 500 ms held response are explicit test-only fault conditions; public Sheets and successful image fetches used the real Worker.
- A separate native re-sync invocation restored saved-page text `17:15` from `STALE_FOR_RESYNC` to `Alpha` while leaving the active page at `17:13`; saved roots still point to page `17:12`. The first test recorder could not intercept Figma's host `postMessage` method. The corrected test-only copy captures the actual production outbound dispatch immediately before it sends/closes; report `17:37` records terminal status `success`. The earlier recorder placeholder remains in evidence rather than being presented as a pass. See `evidence/native-host-resync.json`.
- A real Arial Bold probe created in Desktop Figma is unavailable on the connected Figma host (`hasMissingFont: true`, actual font-load rejection). Running the accepted `syncTextLayer` code against that real text node returned an error/warning without changing its text, geometry, visibility, font, or surrounding structure. A separate native bound fixture is page `17:38`, text `17:39`, with the sentinel `MISSING_FONT_SENTINEL`. No fonts were installed or removed.
- The real-host preflight check exposed an unsupported Page-node property read hidden by permissive desktop behavior. `c8650c6` — `fix: fingerprint only supported node properties` — keeps Page/Document identity separate, reads text styles only on text nodes, and checks optional scene capabilities. Strict getter regressions also prove a changed child still invalidates the review and a refreshed plan applies correctly. The corrected actual preflight produced one blocking `missing-font` issue, zero matched bindings, and no text or structural changes. See `evidence/native-host-missing-font-preflight.json`; this used the actual core against the connected Figma host, not a simulated unavailable font.
- The corrected integrated checkout passed typecheck, recommended lint rules, **984 unit/integration tests in 22 files**, **17 browser tests in three files**, and production build. Logs are in `/private/tmp/sheets-to-layers-host-correction/`. New main asset SHA-256: `c99771d2136de4d9d624841eb81836082deab63c38f3332cd6393419145973ed`; UI is unchanged at `d20bda72e1e640708af5714b59eebd884e864a7ab12e56d12c7c641a588b4855`.
- The second complete host-driver run on `c8650c6`, with a distinct `v2` test namespace, passed all 14 cases. Report `25:64` is preserved in `evidence/native-host-verify-c8650c6.json`. Original reports and the earlier driver copy remain intact. Saved page `25:41` and active page `25:42` are separate. Second-file isolation and the legacy-source suggestion check await the requested Desktop file handoff.
- Production authorization has been requested for the exact prepared Worker proposal: the existing endpoint, quoted-sheet fixes, bounded public-image validation, query-string log redaction, and native rate limit of 600 requests per minute per IP/location. No response or deployment has occurred. Stop on any need for a paid upgrade or different infrastructure. Community publication remains a separate decision.

- Real public-source row changes passed through separate native re-sync invocations: Products grew 3→4 (report `25:65`, Alpha/Beta/Gamma/Delta), shrank 4→2 (report `25:68`), then returned 2→3 (report `31:70`). The shrink preflight listed both groups, four exact removal IDs, and `requiresConfirmation: true`; the bounded test driver used the actual Apply control after reviewing only those known synthetic removals. Both groups' actual text children were read after each phase. Re-sync kept active page `25:42` and saved root `25:41`. An additional unchanged two-row invocation is retained as report `31:69`; it is not the restoration pass. See the three `evidence/native-source-row-*.json` files.
- The temporary edits touched only Products `A4:E5` values. Original Gamma values and the blank fifth row were restored. A bounded native CellData read exactly matches the original values, formats, validation, and chip metadata; Chrome visually shows the original three rows and Saved to Drive. No temporary source values remain.
- Final-source five-run benchmark at `c8650c6` passed: core medians 2.010/30.126/145.541 ms at 100/1,000/5,000 layers; 5,000-layer increase versus original baseline is 74.075 ms, below the 100 ms investigation threshold. UI/network/preview median is 1118.609 ms; worksheet/image/upstream peaks remain 3/4/6, six downloads for six shared URLs, 100 rendered cells for 10,000 rows, and two font loads. Mock-boundary cancellation acknowledged in 1.058 ms with one attempted binding. See `evidence/host-correction/benchmarks.json`; these are local bounds, not actual Figma/network latency claims.

- Native cancellation after Apply starts passed on `c8650c6`: report `31:74`, actual test rectangle `31:73`, terminal `cancelled`, counts 0 changed / 10 unchanged / 2 skipped / 0 failed. The driver waited for a real successful PNG download (5,969 bytes), held its delivery, clicked the actual Cancel control, then released the response. Empty fills and saved document config remained unchanged after release; the row-2 pending image was explicitly skipped. This tests cancellation during image settlement, not just before Apply. The held response is a named test-only fault; production source is unchanged. See `evidence/native-cancel-during-apply.json`.
- The remaining host gate is the second disposable file: confirm its actual INIT has only the legacy URL suggestion and no document config, verify Fetch/Review leave its text/config unchanged, then apply to the sole known text and verify file-owned roots. The expanded isolation command is prepared and statically checked, but has not run because the file is not yet open in Desktop. Browser desktop handoff requests a local-network connection; no browser permission was changed. The earlier user handoff request remains pending.
