{
  "name": "sheets-sync",
  "version": "1.0.0",
  "description": "Figma plugin to sync content from Google Sheets into Figma designs",
  "main": "dist/code.js",
  "scripts": {
    "dev": "concurrently \"npm run watch:code\" \"npm run watch:ui\"",
    "watch:code": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2017 --watch",
    "watch:ui": "node scripts/build-ui.js --watch",
    "build": "npm run build:code && npm run build:ui",
    "build:code": "esbuild src/code.ts --bundle --outfile=dist/code.js --target=es2017 --minify",
    "build:ui": "node scripts/build-ui.js --minify",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "test:unit": "vitest run tests/unit",
    "test:int": "vitest run tests/integration",
    "test:worker": "vitest run tests/integration/worker-harness.test.ts tests/unit/worker-proxy.test.ts",
    "test:browser": "npm run build:ui && vitest run --config vitest.browser.config.ts",
    "lint": "eslint src --ext .ts,.tsx",
    "format": "prettier --write \"src/**/*.{ts,tsx}\""
  },
  "keywords": [
    "figma",
    "plugin",
    "google-sheets",
    "sync"
  ],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "@create-figma-plugin/build": "^4.0.3",
    "@figma/plugin-typings": "^1.100.0",
    "@types/node": "^20.10.0",
    "@typescript-eslint/eslint-plugin": "8.70.0",
    "@typescript-eslint/parser": "8.70.0",
    "@vitest/coverage-v8": "^1.0.4",
    "concurrently": "^8.2.2",
    "esbuild": "^0.19.8",
    "eslint": "^8.55.0",
    "playwright": "1.63.0",
    "prettier": "^3.1.1",
    "typescript": "^5.3.2",
    "vitest": "^1.0.4"
  },
  "dependencies": {
    "@create-figma-plugin/ui": "^4.0.3",
    "preact": "^10.26.4"
  }
}
