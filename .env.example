# Google Sheets API Key
# Required for fetching metadata from Google Sheets API (identifying sheet names).
# Without this, the plugin may fall back to less reliable probing methods.
GOOGLE_SHEETS_API_KEY=your_api_key_here
